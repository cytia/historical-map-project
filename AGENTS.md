# 历史地图项目工作指引

本项目制作可交互的中国历代历史地图。第一版以公元 1600 年左右（万历二十八年前后）为基准，展示晚明两京十三布政使司及相关历史地理信息。

技术栈：React、TypeScript、Vite、MapLibre GL JS、Zustand；首版使用 JSON/GeoJSON；Rust 用于数据校验和转换。数据扩大后再引入 PMTiles/MVT、Axum 与 PostgreSQL/PostGIS。

## 按任务读取规范

- 修改代码、依赖或目录：读 `docs/engineering.md`。
- 采集、转换或修改历史数据：读 `docs/historical-data.md`。
- 创建或修改历史边界：同时读 `docs/boundary-reconstruction.md`。
- 安排工作或改变范围：读 `docs/roadmap.md`。
- 不要为普通局部任务读取全部文档。

## 工作原则

- 先确认需求和影响范围，只修改任务要求的内容。
- 除非用户、项目规范或适用技能明确要求委派或并行代理，否则不启用 subagent。
- UI 变更先确认视觉描述，再实现。
- Bug 先复现，再修复并验证。
- 一个改动保持可运行、可验证、可回滚。
- 不顺手重构，不静默修改版本号。
- 代码、标识符和代码注释使用英文；文档与用户界面可使用中文。
- 注释解释原因，不复述代码行为。
- 优先复用现有模块；同一概念全项目使用同一术语。

## 架构边界

- React 负责界面；MapLibre 负责地图渲染；业务状态不存入地图实例。
- 历史数据与展示样式分离，组件不得内嵌大段历史数据。
- 数据结构以时间有效性为核心，不把“明代”写死在通用模型中。
- 首版优先静态部署；没有明确需求时不增加后端。
- Rust 不用于替代浏览器 UI 或 MapLibre；用于校验、转换、瓦片处理及未来服务端。
- 外部数据先确认许可和来源，不能因“免费”或“公益”而默认可再分发。
- 受限 GIS 多边形不得通过合并、简化或描摹后作为原创边界公开。

## 安全底线

- 密钥、密码、令牌和私钥不得进入代码、文档、日志或 Git。
- 本地秘密使用被忽略的环境文件；CI 和生产使用平台 Secrets。
- 不记录完整请求、响应、授权头、用户内容或本地路径。
- 渲染外部 HTML 必须统一过滤；禁止危险协议和内联脚本。
- 引入依赖前检查用途、维护状态、许可和已知漏洞。
- 发布前检查构建、测试、数据许可、来源归属和敏感信息。

## 完成标准

- 相关构建、测试和数据校验通过。
- 新增历史记录具有来源、有效时间和可信度信息。
- 行为或数据格式变化同步更新相关文档。
- 交付时说明修改内容、验证结果和仍存在的限制。
